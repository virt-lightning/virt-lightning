# How can I add a new image?

Update `refresh.py` and change the `configuration` block to add your image.

Refresh the different files with:

``` shell
./refresh.py
```

Commit the changed files and prepare a Github PR for review.
